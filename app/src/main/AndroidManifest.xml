<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.classapp.tidseries1">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tidseries1">
        <activity android:name=".HomeComedy"></activity>
        <activity android:name=".HomeFamily"/>
        <activity android:name=".HomeHorror"/>
        <activity android:name=".HomeAction"/>
        <activity android:name=".HomeAnimation"/>
        <activity android:name=".HomeRomance"/>

        <activity android:name=".DetailActivity"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>